import React from 'react';
import './Navbar.css'; // Optional: You can create a separate CSS file for navbar styling

const Navbar = () => {
  return (
    <nav className="navbar">
      <div className="navbar-title">Calculator</div>
      {/* Add additional links or buttons here if needed */}
    </nav>
  );
};

export default Navbar;
